# Booking Link Builder

A Streamlit app that converts base hotel booking URLs into dynamic booking links with parameters.

## Features
- Add check-in/check-out dates
- Set adults, children, rooms
- Generate ready-to-use booking links

## Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
